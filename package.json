{
  "name": "sample",
  "version": "0.0.1",
  "description": "Example to use node-rfc in a Docker container.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_PATH=$(npm root -g) node app.js"
  },
  "author": "",
  "license": "Apache",
  "dependencies": {
    "node-rfc": "^3.0.0"
  }
}
